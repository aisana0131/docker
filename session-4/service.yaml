apiVersion: v1
kind: Service
metadata:
  name: simple-service
spec:
  selector:
    app: nginx
  ports:
    - protocol: TCP
      port: 80            # the port that your service is listening to
      targetPort: 80      # 